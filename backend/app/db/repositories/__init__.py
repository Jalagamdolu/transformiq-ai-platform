"""Database repositories package.

Each domain entity will have its own repository class here.
Repositories are the ONLY place that writes SQL — never write queries in
route handlers or engine modules.

Phase 2+: add domain repositories here.
"""
